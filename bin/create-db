#!/usr/bin/env bash
set -euo pipefail

createdb \
  --locale-provider=icu \
  --icu-locale=de-CH \
  --lc-collate=de_CH.UTF-8 \
  --lc-ctype=de_CH.UTF-8 \
  --encoding=UTF8 \
  --template=template0 \
  "$@"
