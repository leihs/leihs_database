include:
  - cider-ci/task-components/database.yml

scripts:

  create-database:
    body: |
      #!/usr/bin/env bash
      set -euo pipefail
      unset PGPORT
      unset PGUSER
      source $LEIHS_DATABASE_DIR/cider-ci/bin/pg_env_set
      createdb -p "${PG10PORT}" -U "${PG10USER}" "$LEIHS_DATABASE_NAME"

  test:
    start_when:
      database has been created:
        script_key: create-database
